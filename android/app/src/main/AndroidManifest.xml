<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 13+ uses READ_MEDIA_* permissions; legacy storage perms removed for release compliance -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- ✨ FIX: จัดกลุ่ม Permission สำหรับ Bluetooth ให้ชัดเจนที่สุด -->

    <!-- Bluetooth base permissions (keep for compatibility; normal permissions on Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    
    <!-- สำหรับ Android 12 (SDK 31) และสูงกว่า -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- ✨ FIX: กุญแจดอกสุดท้าย! ขออนุญาตเข้าถึงตำแหน่งสำหรับทุกเวอร์ชัน
         เพราะบางอุปกรณ์ยังจำเป็นต้องใช้เพื่อสแกนหา Bluetooth ค่ะ -->
    <!-- ขอ Location เฉพาะ Android <= 11 เพื่อความเข้ากันได้กับการสแกน BT บางรุ่น -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" />


    <application
        android:label="MyDent"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
